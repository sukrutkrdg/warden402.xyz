/** Re-export from @warden/core — the calldata decoder now lives in the shared
 *  package (single source of truth). This file is kept for import stability. */
export { decodeCalldata, type DecodedCall } from "@warden/core";
